{
    "name": "sentinela-app-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npx prisma generate --schema=server/prisma/schema.prisma && npm run build --workspace=client",
        "postinstall": "npx prisma generate --schema=server/prisma/schema.prisma || echo 'Prisma generation failed but continuing'"
    },
    "devDependencies": {
        "typescript": "^5.3.3",
        "vite": "^7.2.4"
    },
    "workspaces": [
        "client",
        "server"
    ],
    "dependencies": {
        "@prisma/client": "^5.10.2",
        "prisma": "^5.10.2",
        "lightningcss": "^1.29.1"
    }
}