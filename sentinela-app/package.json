{
    "name": "sentinela-app-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "prisma generate --schema=server/prisma/schema.prisma && npm run build --workspace=client",
        "postinstall": ""
    },
    "engines": {
        "node": "20.x"
    },
    "devDependencies": {
        "prisma": "^5.10.2",
        "vite": "^7.2.4"
    },
    "workspaces": [
        "client",
        "server"
    ],
    "dependencies": {
        "@prisma/client": "^5.22.0"
    }
}
